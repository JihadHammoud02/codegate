# Big example project

This is a larger example codebase used to smoke-test CodeGate on:

- multiple packages/modules
- unit tests across several files
- a small CLI entrypoint

Run with:

```bash
cd examples/big_project
codegate run contract.yaml --verbose
```
