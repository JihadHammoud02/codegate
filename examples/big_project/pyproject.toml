[project]
name = "big-project"
version = "0.1.0"
description = "Larger example project for CodeGate"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
